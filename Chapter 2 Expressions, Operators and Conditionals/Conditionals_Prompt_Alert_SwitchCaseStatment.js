// Conditionals:

        // *) if(){}

        // *) if(){}
            //else{}
            
        // *) if(){}
            //else if(){}
            //else if(){}
            //else{}

// Ternary Operator (?): same like if(){}else{} 

// Alert : Shows you an alert and it is a browser feature.
// Prompt : Asks from you some value in it and then store that value in the given variable.

// SwitchCaseStament: 

        // const expr = 'Papayas';
        // switch (expr) {
        //   case 'Oranges':
        //     console.log('Oranges are $0.59 a pound.');
        //     break; // if the above case (case 'Oranges) will meet the given case then it will execute (console.log('Oranges are $0.59 a pound.');) code and then this break will break the code otherwise if we remove this break the code below before the below break will also get printed in the console and this is the difference between conditionals and switch case that in conditionals after meeting the given condition the code will stop but in switch case the code will also run all the cases below the case that met the given condition along with default (default:) if (break;) is not written means for breaking (for stopping) the code we use (break;).
        //   case 'Mangoes':
        //   case 'Papayas':
        //     console.log('Mangoes and papayas are $2.79 a pound.');
        //     // Expected output: "Mangoes and papayas are $2.79 a pound."
        //     break;
        //   default:
        //     console.log(`Sorry, we are out of ${expr}.`);
        // }

        // If no case meet the given expression then the code inside default will run maybe( 99% sure maybe ).

            