// Overview Of Video:
//Functions are used for separation of logic.
//

// Functions: 

        // A JavaScript function is a block of code designed to perform a particular task.

        // Syntax: 

                // function name of function(parameters or input// both are same){
                    // code
                // }

        // Example: 

                // function abc(parameter1, parameter2 //Note: Function with parameters){
                    //code // Note: Here the parameters behave as local variables
                // }

                //abc(7, 8) // Note: Function invocation.

                // Note: If a function takes parameters generally this means that this function will return something in result but it is not the case everytime. Sometimes the function takes parameters but don't return anything, sometimes it doesn't take any parameters but return something. When we mention return keyword inside the function then the function will definitely return something.

                // Practice Set Question: 

                    // Q) Write a function to find mean of 5 numbers..

                    // Ans) let meanOf5Nums = (a, b, c, d, e) => {
                        // return (a + b + c + d + e)/5
                    // }

                    // console.log(meanOf5Nums(1, 2, 3, 4, 5));




