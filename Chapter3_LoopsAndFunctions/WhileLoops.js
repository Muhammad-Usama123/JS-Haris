// While Loops:

        // *) While Loop:

            // Loops a block based on a specific condition.

            // Syntax: while(condition){
                //Code to be executed
            // }

            // Example: let n=4
                      //let i=0

                      // while (i<n){
                        // console.log(i);
                        // i++;
                    //   }

            // In the above example the above loop will run until the condition "(i<n)" is met, whenever this condition becomes false the loop will stop. 

            // Note: If the condition never becomes false in this type of while loop, the loop will never end and this might crash the runtime.

            // Practice Set Question: 

                // Q) Write a program to print "try again" until the user enters the correct number.

                // let i = prompt("Enter the correct number..")
                // i = Number.parseint(a);

                // while (i === 23) {
                    // console.log("Try again...")
                // }

                                // OR 

                // let cn = 4;
                // let i;
                
                // while(i != cn){
                    // console.log("Try again...")
                    // i = prompt("Enter the correct number...")
                // }

        // *) Do While Loop:

            // While loop variant which runs atleast once.

            // Syntax: do {
                // Code to be executed //Note: This code will atleast execute one time.
            // }
            // while(condition)