// 1) ECMAScript is the standard followed by Javascript.

// 2) In facebook, whatsapp, Twitter etc. content changes (like when we receive message etc. in these apps or websites) without refreshing the page, this is because of Javascript. So we use Javascript so we can make interactive web apps.


// 2) How to execute Javascript:

    // *) Through browser
    // *) Through runtime(like node js(we can install it in our computer and then execute the javascript code))
    // *) Through script tag inside HTML.
    // replit is also a way to execute a javascript program.